# 应用名称
spring:
  application:
    name: oa-config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/js20166098/spring-cloud-config
          #          default-label: master
          #          search-paths: /**
          username: 18845721152  #账号密码写真实的快一些我觉得，不使用也能访问有点慢
          password: APOTOX4869
        default-label: master
server:
  port: 8999 #服务端口